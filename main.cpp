
#include "grov.hpp"

using namespace std;

int main(){

    // Grov grovSolver;
    // auto var1 = grovSolver.addVar(0, 1, CONTINUOUS);    
    // auto var2 = grovSolver.addVar(0, 1, BINARY);
    // LinVar linvar;
    // linvar += 1000 *var1;
    // linvar -= 2345 * var2;
    // linvar += 11;

    // grovSolver.addConstrant(var1 + var2 <= 1);
    // grovSolver.addConstrant(linvar <= 2);
    
    // grovSolver.view();

    // cout << linvar << endl;
    // return 0;
}